{
  "name": "faststock-frontend",
  "version": "0.1.0",
  "scripts": {
    "tailwind:build": "npx @tailwindcss/cli -i ./src/input.css -o ./public/css/output.css --watch",
    "serve": "live-server public --port=3000 --host=0.0.0.0",
    "dev": "npm run tailwind:build & npm run serve"
  },
  "devDependencies": {
    "tailwindcss": "4.1.0",
    "live-server": "^1.2.1",
    "@tailwindcss/cli": "^4.1.0"
  }
}
