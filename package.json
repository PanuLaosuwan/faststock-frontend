{
  "name": "faststock-frontend",
  "version": "0.1.0",
  "scripts": {
    "tailwind:build": "tailwindcss -i ./src/input.css -o ./public/css/output.css --watch",
    "serve": "live-server public --port=3000 --host=0.0.0.0",
    "dev": "sh -c \"npm run tailwind:build & npm run serve\""
  },
  "devDependencies": {
    "tailwindcss": "4.1.0",
    "live-server": "^1.2.1"
  }
}
